# Development Profile Configuration

# Database Configuration - Local MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/tripease_db
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Development Settings
spring.jpa.show-sql=true

# SMTP Configuration - Gmail (for development testing)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:punithreddy141@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.com.example.tripease=DEBUG
